<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SPKN | EDIT PROFILE</title>
    <style>
        /* Global styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 120vh;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        /* Form container */
        .form-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            margin: 20px;
        }

        /* Form labels and inputs */
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #333;
        }

        input[type="text"],
        input[type="number"],
        select {
            width: 95%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        /* Submit button */
        button[type="submit"] {
            width: 100%;
            padding: 10px;
            background-color: #28a745;
            color: #fff;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }

        button[type="submit"]:hover {
            background-color: #218838;
        }

        /* Back to home link */
        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #007bff;
            text-decoration: none;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        /* Responsive design */
        @media (max-width: 600px) {
            body {
                padding: 20px;
            }

            .form-container {
                padding: 15px;
                box-shadow: none;
                border: 1px solid #ddd;
            }

            h1 {
                font-size: 1.5em;
            }

            input[type="text"],
            input[type="number"],
            select {
                padding: 8px;
                font-size: 14px;
            }

            button[type="submit"] {
                padding: 8px;
                font-size: 14px;
            }
        }
    </style>
</head>

<body>
    <div class="form-container">
        <h1>Edit Profile</h1>

        <!-- Form for editing the item, with pre-filled data -->
        <form action="/edit-item/<%= item._id %>" method="POST">
            <label for="name">Name:</label>
            <input type="text" name="name" value="<%= item.name %>" required>

            <label for="age">Age:</label>
            <input type="number" name="age" value="<%= item.age %>" required>

            <label for="salary">Salary:</label>
            <input type="number" name="salary" value="<%= item.salary %>" required>

            <label for="jadagam">Jadagam:</label>
            <input type="text" name="jadagam" value="<%= item.jadagam %>" required>

            <label for="gender">Gender:</label>
            <select name="gender" required>
                <option value="Male" <%= item.gender === 'Male' ? 'selected' : '' %>>Male</option>
                <option value="Female" <%= item.gender === 'Female' ? 'selected' : '' %>>Female</option>
                <option value="Other" <%= item.gender === 'Other' ? 'selected' : '' %>>Other</option>
            </select>

            <label for="profession">Profession:</label>
            <select name="profession" required>
                <option value="Engineer" <%= item.profession === 'Engineer' ? 'selected' : '' %>>Engineer</option>
                <option value="Doctor" <%= item.profession === 'Doctor' ? 'selected' : '' %>>Doctor</option>
                <option value="Teacher" <%= item.profession === 'Teacher' ? 'selected' : '' %>>Teacher</option>
                <option value="Other" <%= item.profession === 'Other' ? 'selected' : '' %>>Other</option>
            </select>

            <button type="submit">Update Profile</button>
        </form>

        <a href="/" class="back-link">Back to Home</a>
    </div>
</body>

</html>
